const {
  TRAKT_API_URL,
  TRAKT_CLIENT_ID,
  TRAKT_CLIENT_SECRET,
  TRAKT_REDIRECT_URI
} = process.env;

export {
  TRAKT_API_URL as url,
  TRAKT_CLIENT_ID as key,
  TRAKT_CLIENT_SECRET as secret,
  TRAKT_REDIRECT_URI as redirect
};
